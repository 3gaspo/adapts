CLUSTER EXPERIMENT STATUS
Updated: 2026-08-06

PREVIOUS TS-IFA PILOT (HISTORICAL)
Run/profile: EXPERIMENT_MODE=full TS-IFA training with DATASETS_CSV=Electricity,
DISTANCE_SPACES_CSV=raw, and NEIGHBORS_CSV=3 (a narrowed subset of the default
full profile)
Job ID/dependency: 42149 / none recorded
Artifacts synchronized locally: yes
Analyzed locally: yes
Completion note: All three Electricity settings (168:24, 336:48, and 504:168)
completed, the job emitted its completion marker, and stderr is empty. Lightweight
metrics, manifests, histories, and training plots are synchronized locally.
Analysis note: On held-out T3, the neural rooter was worse than vanilla in all
three settings (equal-setting mean nMSE improvement -27.42%); the ridge rooter
won only 168:24 and averaged -14.58%. Both rooters improved over vanilla on T2,
so the reversal on T3 shows substantial validation-to-evaluation overfitting.
Checkpoints and NumPy predictions remain cluster-only as intended.
Contract note: these combined-rooter TS-IFA artifacts predate the 2026-08-04
four-variant contract and are historical only. Current variants are joint ridge,
joint neural, meta ridge, and meta neural, each with an isolated result folder.
Rerun before current-model comparisons; the existing matching extraction
payloads remain reusable and are not regenerated by a TS-IFA front.

LATEST TS-IFA PILOT
Run/profile: EXPERIMENT_MODE=full, DATASETS_CSV=Electricity,
DISTANCE_SPACES_CSV=raw, NEIGHBORS_CSV=3, joint-ridge variant
Job ID/dependency: 42300 / none recorded
State: completed successfully; 3/3 settings and final job marker present.
Artifacts synchronized locally: yes, excluding cluster-only checkpoints and
NumPy predictions as intended.
Analyzed locally: yes.
Analysis note: equal-setting held-out T3 nMSE improvement is +0.04%, versus
-14.58% for the historical combined-rooter ridge result. The updated rooter
improves nMSE by +3.26% at 168:24 and +0.58% at 336:48, but loses -3.72% at
504:168. Raw MSE and MAE worsen at all three settings. T2 selected step 10000
in every run; the long-horizon T2 gain still reverses on T3. Joint training uses
3H free ridge coefficients with adjacent-horizon smoothness 0.01.

LATEST 30K JOINT-RIDGE PILOTS (OPERATIONAL CHECK ONLY)
1. Job 42493: Electricity, raw Euclidean K=3, three full-profile settings,
   RESULTS_ROOT=outputs/adaptation_results/ts_ifa_joint_ridge_raw_30k.
2. Job 42494: Electricity, instance Euclidean K=3, three full-profile settings,
   RESULTS_ROOT=outputs/adaptation_results/ts_ifa_joint_ridge_instance_30k.
State: both jobs completed successfully on 2026-08-06. Both stderr files are
empty, all 6/6 configurations reached step 30000, all six current joint-ridge
result and prediction manifests are present, and all six non-empty training
plots were synchronized. Checkpoints and NumPy predictions remain cluster-only
as intended. The runs have been checked operationally but their scores have not
yet been analyzed.

LATEST ANALYZED SCREEN
Run/profile: EXPERIMENT_MODE=screen workflow
Job ID/dependency: 42058 / none recorded
Artifacts synchronized locally: yes
Analyzed locally: yes
Analysis note: The complete screen was analyzed, its canonical cov/avgy artifact
names were migrated without recomputation, and the nine selected pipelines are
recorded in SWEEP_CANDIDATES.txt.

LATEST COMPLETED K-ABLATION
Run/profile: k_ablation.slurm with the nine SWEEP_CANDIDATES.txt pipelines and
K=1,3,5,10,15,20.
Job ID/dependency: 42332 / none recorded
Artifacts synchronized locally: yes.
Analyzed locally: yes.
State: completed successfully through final tables on 2026-08-05; stderr is
empty. All 72/72 gate configurations used the current 300-tree ceiling, all
144/144 baseline configurations completed, and both full and average tables
were written. These configurations remain reusable. K=100 is the only pending
extension.

LATEST FORMULATION ABLATIONS
1. Job 42301: convex_baselines_ablation.slurm, completed successfully.
2. Job 42302: delta_baselines_ablation.slurm, completed successfully.
3. Job 42303: catboost_ablation.slurm, completed successfully.
Artifacts synchronized locally: yes.
Analyzed locally: yes.
Validation note: stderr is empty, every evaluation group reached 12/12, all 24
aggregate manifests per profile are valid, complete fitting splits were used,
and the unchanged controls exactly reproduce the screen metrics.
Analysis note: every convex and delta-ridge formulation is worse on average than
its matching shared-ridge control. The hard shared CatBoost regressors remain
the best learned CatBoost forms; classifier, soft-mixture, and horizon variants
are not promoted. The selected list therefore remains the same nine pipelines
in SWEEP_CANDIDATES.txt, including cov K=1 and avgy K=3 hard shared regressors
and the inexpensive shared Bayes cov control.

TABLE FIX
The CatBoost run completed both K=3 avgy and K=1 cov groups, but its generated
table showed only the last K group because sourced runners overwrote the
orchestrator's NEIGHBORS array. The orchestrator now preserves the complete grid
separately in SELECTED_NEIGHBORS. Shell syntax and the profile regression check
pass. Future mixed-K tables, including the K ablation, enumerate every K.

TABLE ARTIFACT CONTRACT FIX
Table construction now reads baseline, gate, TS-IFA, and Cross-RAG metrics only
through their current result manifests and verifies complete selected
dataset/setting/model/pipeline coverage before writing tables. Selected
pipelines remain family-qualified internally. Historical combined ts_ifa/TS-IFA
folders were removed locally; isolated joint/meta variant outputs are the only
supported TS-IFA contract. Existing current baseline, gate, and isolated TS-IFA
results remain reusable. Cross-RAG outputs made before its new completion
manifest and positive-window metric must be rerun; no such completed run is
recorded here.

TABLE COEFFICIENT EXPORT
The shared table stage now exports exact signed fitted baseline coefficients as
CSV and imshow PNG files, including horizon-wise and convex-family parameters.
All profile and ablation fronts now reuse complete baseline/gate results by
default, so resubmitting an already-completed front skips fitting and rebuilds
tables plus coefficient plots. Set SKIP_COMPLETE=false only for an intentional
fit rerun.

K=100 EXTENSION
The K-ablation grid now adds K=100. Resubmitting k_ablation.slurm reuses all
complete K=1,3,5,10,15,20 configurations, computes only missing K=100
extractions and selected baseline/gate results, and then rebuilds the tables,
coefficient heatmaps, and one combined average nMSE-improvement-versus-K plot
containing every selected candidate. K=100 has not yet been submitted.

MAINTENANCE
Update this file after submission, completion, artifact synchronization, and analysis.
