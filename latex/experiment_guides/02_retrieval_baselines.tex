\documentclass[10pt]{article}
\input{common}
\title{Adaptation Experiment 2: Retrieval Baselines}
\author{}
\date{}
\begin{document}
\maketitle
\enlargethispage{5\baselineskip}

\section{Theoretical overview and goal}
Let $(x_c^{(j)},y_c^{(j)})_{j=1}^{k}$ be past windows retrieved for query $x$.
After transferring each neighbor to the query scale, distance weights are
\[
 w_j=\frac{\exp(-\tilde d_j)}{\sum_{r=1}^{k}\exp(-\tilde d_r)},
 \qquad \tilde d_j=\frac{d_j-\min_r d_r}{\operatorname{sd}(d)+\varepsilon}.
\]
The simplest memory forecast is $\hat y_K=\sum_jw_jy_c^{(j)}$.  If the frozen
backbone's neighbor residual is $e_c^{(j)}=y_c^{(j)}-f(x_c^{(j)})$, residual
adaptation instead uses $\hat y_R=\hat y_0+\sum_jw_je_c^{(j)}$.  Scalar mixtures
and ridge regressions learn how strongly these signals should correct the
vanilla forecast.

\goal{Determine whether retrieval contains useful predictive information before
introducing learned gates or the TS-IFA neural adapter, and identify whether
neighbor horizons or neighbor residuals are the more effective memory.}

\section{Implementation details}
Shared extraction uses \Tzero as the datastore, \Tone to fit baseline
coefficients, \Ttwo for oracle diagnostics, and untouched \Tthree for final
evaluation.  Online retrieval is period-aligned: the store grows with time but
uses only eligible history.  The current sweep uses Euclidean distance, raw and
instance-normalized spaces, $k\in\{1,3,10\}$, daily period/stride 24, and at most
30,000 store windows.

Small uses the three-dataset RevIN reproduction profile; full adds the six-dataset
Chronos and 512--64 Cross-RAG comparisons; ultra adds TabPFN-TS. Atomic
manifests prevent consumers from loading partial payloads.

\begin{table}[h]
\centering
\begin{tabular}{p{.31\linewidth}p{.61\linewidth}}
\toprule
Family & Compared predictions \\
\midrule
Direct & Vanilla backbone; context-conditioned backbone \\
Neighbor & Weighted/mean neighbor horizon; weighted neighbor residual \\
Scalar fit & Convex horizon mixture; scaled residual correction \\
Ridge fit & Shared or horizon-specific residual, horizon, and full mixtures \\
\bottomrule
\end{tabular}
\end{table}

Ridge features are RMS-standardized without centering, and coefficients use
$\ell_2=10^{-3}$. Exact float64 sufficient statistics are accumulated in
bounded chunks instead of a complete design matrix. An optional seeded \Tone
fit cap defaults to unlimited; final \Tthree scoring always remains complete.

\textbf{Code:} \code{src/experiments/extraction.py};
\code{src/adaptors/baselines/evaluate.py}.\\
\textbf{Launchers:} \code{extraction.slurm}; \code{baselines.slurm}.

The launcher also requests \code{--fit-baselines-on-eval}.  Methods suffixed
\code{eval\_fit} are explicitly optimistic \Tthree in-sample fits: they are
diagnostic upper bounds, not causal test results. Their independent fit cap
also leaves scoring complete. Primary tables use pre-\Tthree coefficients.

The baseline job depends on extraction; tables report per-backbone details and
equal-configuration means.

\section{Interpretation}
Residual wins indicate backbone-bias correction; neighbor-horizon wins indicate
local analog forecasting. A large valid--\code{eval\_fit} gap motivates gating.
\end{document}
