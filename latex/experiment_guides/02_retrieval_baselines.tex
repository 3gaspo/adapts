\documentclass[10pt]{article}
\input{common}
\title{Adaptation Experiment 2: Retrieval Baselines}
\author{}
\date{}
\begin{document}
\maketitle

\section{Signals and leakage-free retrieval}
For query date $s$, the exact window convention is
\[
 X_s=(s-L,s],\qquad Y_s=(s,s+H].
\]
Let $V=f(X_s)$ be the vanilla backbone forecast and let $C$ be the same
backbone conditioned on retrieved examples. For neighbor $j$, let $r_j$ be
its last look-back date, $Y_j$ its observed future, and $N_j=f(X_{r_j})$ its
vanilla forecast. All neighbor values are transferred to the query's level
and scale before use. Distance weights are
\[
 w_j=\frac{\exp(-\widetilde d_j)}{\sum_{\ell=1}^{k}
 \exp(-\widetilde d_\ell)},\qquad
 \widehat Y_h=\sum_{j=1}^{k}w_jY_{j,h}.
\]

In period-aligned retrieval, the last dates of the look-backs satisfy
$s-r_j\equiv0\pmod P$. In fixed mode every $(X_{r_j},Y_{r_j})$ lies wholly
inside \Tzero. In online mode a neighbor is eligible only when
$r_j+H\leq s$, so its complete future is observed by query time. A retrieved
future may overlap the query look-back, which is already observed, but it can
never overlap $Y_s$. These conditions prevent target leakage.

\goal{Compare all useful direct combinations of the available information
$V,C,Y_j,N_j$ before introducing a neural adapter.}

\section{Anchored models}
Every fitted forecast is written directly as $V+\text{linear signals}$.
There is no intercept and no design based only on differences such as
$Y_j-N_j$. The word ``residual'' below is only the agreed name for the model
containing both $Y_j$ and $N_j$.

A \emph{shared} model has one coefficient vector used at every horizon.
A \emph{horizon} model fits $H$ independent regressions. Each of those $H$
regressions has the same number of coefficients as the shared regression;
the total coefficient count is therefore multiplied by $H$.

\small
\begin{longtable}{p{.27\linewidth}p{.55\linewidth}r}
\toprule
Artifact name & Forecast at horizon $h$ & Coefficients \\
\midrule
\endhead
\code{vanilla} & $V_h$ & 0\\
\code{context\_forecast} & $C_h$ & 0\\
\code{aggr\_y} & $\widehat Y_h$ & 0\\
\code{y\_mean} & $k^{-1}\sum_jY_{j,h}$ & 0\\
\midrule
\code{aggr\_y\_mix\_shared}
  & $(1-\lambda)V_h+\lambda\widehat Y_h$ & 1\\
\code{aggr\_y\_mix\_horizon}
  & $(1-\lambda_h)V_h+\lambda_h\widehat Y_h$ & $1$ per $h$\\
\midrule
\code{context\_ridge\_shared}
  & $V_h+aV_h+bC_h$ & 2\\
\code{context\_ridge\_horizon}
  & $V_h+a_hV_h+b_hC_h$ & 2 per $h$\\
\code{aggr\_y\_ridge\_shared}
  & $V_h+aV_h+b\widehat Y_h$ & 2\\
\code{aggr\_y\_ridge\_horizon}
  & $V_h+a_hV_h+b_h\widehat Y_h$ & 2 per $h$\\
\code{y\_ridge\_shared}
  & $V_h+aV_h+\sum_jb_jY_{j,h}$ & $1+k$\\
\code{y\_ridge\_horizon}
  & $V_h+a_hV_h+\sum_jb_{j,h}Y_{j,h}$ & $1+k$ per $h$\\
\code{cov\_y\_ridge\_shared}
  & $V_h+aV_h+bC_h+\sum_jc_jY_{j,h}$ & $2+k$\\
\code{cov\_y\_ridge\_horizon}
  & $V_h+a_hV_h+b_hC_h+\sum_jc_{j,h}Y_{j,h}$ & $2+k$ per $h$\\
\code{cov\_horizon\_ridge\_shared}
  & $V_h+aV_h+bC_h+c\widehat Y_h$ & 3\\
\code{cov\_horizon\_ridge\_horizon}
  & $V_h+a_hV_h+b_hC_h+c_h\widehat Y_h$ & 3 per $h$\\
\code{residual\_ridge\_shared}
  & $V_h+aV_h+\sum_jb_jY_{j,h}+\sum_jc_jN_{j,h}$ & $1+2k$\\
\code{residual\_ridge\_horizon}
  & $V_h+a_hV_h+\sum_jb_{j,h}Y_{j,h}+\sum_jc_{j,h}N_{j,h}$ & $1+2k$ per $h$\\
\code{full\_ridge\_shared}
  & $V_h+aV_h+bC_h+\sum_jc_jY_{j,h}+\sum_jd_jN_{j,h}$ & $2+2k$\\
\code{full\_ridge\_horizon}
  & $V_h+a_hV_h+b_hC_h+\sum_jc_{j,h}Y_{j,h}+\sum_jd_{j,h}N_{j,h}$ & $2+2k$ per $h$\\
\bottomrule
\end{longtable}
\normalsize

The two lambda models are the only retained convex mixtures. Their
least-squares estimate is projected onto $[0,1]$ with
$\lambda\leftarrow\min(1,\max(0,\lambda_{\rm LS}))$; the horizon variant
does this independently for each $\lambda_h$. Ridge coefficients are
unconstrained.

\section{Model-local split and validation protocol}
Extraction fixes only
\[
 \Tzero\;(30\%),\qquad \Tone+\Ttwo\;(50\%),\qquad \Tthree\;(20\%),
\]
and writes \code{adapt} and \code{eval} payloads. For these baselines,
\code{adapt} is split chronologically by whole query dates, with its final
20\% assigned to \Ttwo by default.

\begin{enumerate}
\item Accumulate exact float64 sufficient statistics on \Tone.
\item For every ridge family, fit every
$\alpha\in\{0,10^{-6},10^{-5},10^{-4},10^{-3},10^{-2},10^{-1},1,10\}$ and
select the value with lowest \Ttwo nMSE.
\item Refit that family on all of \Tone+\Ttwo using the selected $\alpha$.
\item Fit the lambda models directly on \Tone+\Ttwo because they have no
regularization hyperparameter to validate.
\item Score every selected model once on the complete untouched \Tthree.
\end{enumerate}

Features are RMS-standardized without centering, so a zero coefficient still
means no added signal. Chunked sufficient statistics change memory usage, not
the fitted objective. Optional fit-only sample caps never subsample final
\Tthree scoring. Optimistic \code{eval\_fit} variants remain available behind
an explicit flag for appendix diagnostics and are disabled by default.

\textbf{Code:} \code{src/experiments/extraction.py};
\code{src/adaptors/baselines/evaluate.py}.\\
\textbf{Launchers:} \code{extraction.slurm}; \code{baselines.slurm};
\code{horizon\_baselines\_ablation.slurm}.

\section{Screening and ablation sequence}
The candidate screen uses Chronos-2 on
\[
 \mathcal D=\{\text{Electricity, Traffic, Solar, Exchange}\},
 \qquad
 \mathcal S=\{168{:}24,336{:}48,504{:}168\},
\]
with $k\in\{1,3\}$ and both raw and instance-normalized Euclidean retrieval.
Fourier-amplitude retrieval is also implemented as an optional ablation space;
it standardizes each look-back before taking the FFT magnitude and does not
enter the primary screen.
The selected-winner full/ultra benchmark retains these settings without
reopening candidate selection.
The primary screen contains the direct forecasts and every shared fitted
baseline in the table above. It excludes every \code{*\_horizon} baseline;
those models are not candidates for the main comparison.
Here a setting is one dataset--$(L,H)$ pair, giving
$|\mathcal D||\mathcal S|=12$ settings. A candidate model is the complete
pipeline (formula, retrieval normalization, $k$). Each such pipeline is ranked
by its unweighted mean of the 12 setting-level percentage improvements over
$V$; there is no averaging over $k$ or normalization. Complete winner names
are copied manually from the ranking for three separate studies:
\[
\begin{array}{ll}
\text{$k$ ablation:} & k\in\{1,3,5,10,15,20\},\quad (\mathcal D,\mathcal S),
 \text{ retaining formula/normalization},\\
\text{$H$ ablation:} & L=504,\quad H\in\{24,168,504\},
 \text{ retaining formula/normalization/$k$},\\
\text{$L$ ablation:} & H=24,\quad L\in\{24,168,504\},
 \text{ retaining formula/normalization/$k$}.
\end{array}
\]
The companion positive-window table averages, with the same configuration
weighting, the saved percentage of complete windows whose horizon-averaged MSE
is strictly lower than vanilla; no per-window metric vector is stored.
Per-horizon parameterization is a separate ablation. The root front
\code{horizon\_baselines\_ablation.slurm} accepts complete shared winners in
\code{SHARED\_BASELINE\_WINNERS\_CSV}; for every selected pipeline it evaluates
both the shared model and its corresponding \code{*\_horizon} model under the
same retrieval normalization and $k$. The current defaults retain the best
shared convex mixture, aggregated-target ridge, and individual-$Y$ ridge from
the completed screen. Its results are written under the distinct
\code{horizon\_baselines\_ablation} profile and are reported only as an
ablation.

The four original ETT panels and Weather are excluded from $\mathcal D$ because their channels mix
unlike physical quantities; nearest neighbors across those channels may be
semantically irrelevant even after scale normalization. The separate root
front \code{mixed\_quantity\_ablation.slurm} transfers only selected complete
screen winners to
\[
 \mathcal D_{\rm mixed}=\{\text{ETTh1, ETTh2, ETTm1, ETTm2, Weather}\}
\]
and writes a distinct \code{mixed\_quantity\_ablation} result profile. These
results are an ablation and never contribute to primary winner selection.
All variates are retained for these original ETT panels and Weather, as well as
for Exchange in the primary set; constant-window counts are reported but are
not converted into dataset-level exclusions.

Every ablation consumes selected complete screen winners. The $k$ ablation is
the only in-project exception to the ordinary $k\in\{1,3\}$ policy; its wider
grid is evaluated only for those selected pipelines. Cross-RAG separately
retains the released method's prescribed $k=15$ comparison.

The final \code{benchmark.slurm} comparison transfers only selected complete
screen winners to $\mathcal D$ plus \code{ETT\_T\_1H}, \code{ETT\_L\_1H},
\code{ETT\_T\_15T}, and \code{ETT\_L\_15T}, while retaining the same
$\mathcal S$. The selected winner name fixes its formula, normalization,
distance, $k$, and retrieval mode. Full uses Chronos-2; ultra adds backbones
without changing datasets, settings, or candidate pipelines. These merged ETT
panels retain only one physical quantity; 15-minute extraction uses a 96-step
daily retrieval period.

The Cross-RAG comparison is not crossed with $\mathcal S$: it separately uses
Chronos-Bolt, $L=512$, $H=64$, $k=15$, and min--max/cosine X-space retrieval.
Only the winning in-project candidate is fitted in that profile.

\section{Interpretation}
\code{aggr\_y} tests analog forecasting directly; \code{context} tests whether
the backbone can consume retrieved context. The individual-$Y$ models test
whether learned neighbor-specific weights beat the fixed distance aggregation.
\code{residual} tests whether neighbor forecasts add information about neighbor
reliability, and \code{full} is the complete $V,C,Y_j,N_j$ model. All claims
must be based on the same complete \Tthree queries and compared with $V$.
\end{document}
