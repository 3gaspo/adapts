# Retired K=100 extraction runs

Archived: 2026-08-18

This batch contains the three schema-1 raw Euclidean online `K=100` extraction
runs produced or attempted by job 43589 for Electricity. The `168:24` and
`336:48` runs completed; the `504:168` run was interrupted by an out-of-memory
failure before evaluation or table construction.

The project no longer includes `K=100` in the active K study. No baseline or
gate result was produced from these payloads, and no active downstream manifest
or current report referenced them before archival. Their original relative
paths, manifests, payloads, and completed-run selection indexes are preserved
below `runs/`; the complete job-43589 stdout/stderr pair is under `logs/`.
